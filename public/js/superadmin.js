(()=>{
  let d=null, filtered=[];
  async function load(){try{const me=FCK.state.user;if(!me||me.role!=='superadmin'){location.href='/index.html';return}d=await FCK.api('/api/superadmin');render()}catch(e){if(e.status===403)location.href='/index.html';else FCK.toast(e.message)}}
  function render(){renderStats();applyFilter();renderResets();renderAudit();document.getElementById('createAdminBtn')?.addEventListener('click',openCreateAdmin);document.getElementById('superSearch')?.addEventListener('input',applyFilter)}
  function renderStats(){const users=d.users||[],active=users.filter(u=>u.status==='active').length,admins=users.filter(u=>u.role==='admin').length,members=users.filter(u=>u.role==='member').length,pending=(d.reset_requests||[]).length;document.getElementById('superStats').innerHTML=`<div class="stat"><small>Comptes actifs</small><strong>${active}</strong></div><div class="stat"><small>Administrateurs</small><strong>${admins}</strong></div><div class="stat"><small>Utilisateurs</small><strong>${members}</strong></div><div class="stat"><small>Réinitialisations en attente</small><strong>${pending}</strong></div>`}
  function applyFilter(){const q=(document.getElementById('superSearch')?.value||'').toLowerCase().trim();const src=d?.users||[];filtered=q?src.filter(u=>[u.full_name,u.email,u.role,u.plan,u.status].some(v=>String(v||'').toLowerCase().includes(q))):src;renderUsers()}
  function renderUsers(){const el=document.getElementById('superUsers');el.innerHTML=`<table><thead><tr><th>Nom</th><th>E-mail</th><th>Rôle</th><th>Statut</th><th>Plan</th><th>Expiration</th><th>Actions</th></tr></thead><tbody>${filtered.map(u=>`<tr><td>${FCK.esc(u.full_name)}</td><td>${FCK.esc(u.email)}</td><td><span class="badge badge-blue">${FCK.esc(u.role)}</span></td><td><span class="badge ${u.status==='active'?'badge-green':'badge-red'}">${FCK.esc(u.status)}</span></td><td><span class="badge ${u.plan==='free'?'badge-orange':'badge-green'}">${FCK.esc(u.plan)}</span></td><td>${FCK.fmtDate(u.plan_expires_at)}</td><td>${u.role==='superadmin'?'<span class="hint">Compte protégé</span>':`<div class="actions"><button class="btn btn-sm btn-ghost" data-status="${u.id}">${u.status==='active'?'Désactiver':'Activer'}</button><button class="btn btn-sm btn-outline" data-plan="${u.id}">Plan</button><button class="btn btn-sm btn-outline" data-reset="${u.id}">Réinit. MDP</button>${u.role==='member'?`<button class="btn btn-sm btn-ghost" data-access="${u.id}">Accès</button>`:''}<button class="btn btn-sm btn-danger" data-delete="${u.id}">Supprimer</button></div>`}</td></tr>`).join('')}</tbody></table>`;
    el.querySelectorAll('[data-status]').forEach(b=>b.addEventListener('click',()=>{const u=filtered.find(x=>x.id===b.dataset.status);post('set-status',{id:u.id,status:u.status==='active'?'disabled':'active'},'Statut mis à jour.')}));
    el.querySelectorAll('[data-plan]').forEach(b=>b.addEventListener('click',()=>openPlan(filtered.find(x=>x.id===b.dataset.plan))));
    el.querySelectorAll('[data-reset]').forEach(b=>b.addEventListener('click',()=>openReset(filtered.find(x=>x.id===b.dataset.reset))));
    el.querySelectorAll('[data-access]').forEach(b=>b.addEventListener('click',()=>openAccess(filtered.find(x=>x.id===b.dataset.access))));
    el.querySelectorAll('[data-delete]').forEach(b=>b.addEventListener('click',()=>{const u=filtered.find(x=>x.id===b.dataset.delete);if(confirm(`Supprimer définitivement le compte de ${u.full_name} ?`))post('delete-user',{id:u.id},'Compte supprimé.')}));
  }
  async function post(action,payload,msg){try{const r=await FCK.api('/api/superadmin',{method:'POST',body:JSON.stringify({action,...payload})});FCK.toast(r.message||msg||'Opération effectuée.');await load()}catch(e){FCK.toast(e.message)}}
  function openCreateAdmin(){FCK.modal({title:'Ajouter un Administrateur',html:`<form id="createAdmin" class="form-grid"><div class="field"><label>Nom complet *</label><input class="input" name="full_name" required></div><div class="field"><label>Téléphone</label><input class="input" name="phone"></div><div class="field full"><label>E-mail *</label><input class="input" type="email" name="email" required></div><div class="field full"><label>Mot de passe temporaire *</label><input class="input" type="password" name="password" minlength="8" required></div><div class="alert alert-info field full">L’Administrateur sera créé sur le plan Free de 10 jours et devra changer son mot de passe temporaire.</div><div class="field full"><button class="btn btn-orange" type="submit">Créer l’Administrateur</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#createAdmin').addEventListener('submit',async e=>{e.preventDefault();try{const r=await FCK.api('/api/superadmin',{method:'POST',body:JSON.stringify({action:'create-admin',...Object.fromEntries(new FormData(e.currentTarget))})});FCK.toast(r.message);close();await load()}catch(err){FCK.toast(err.message)}})})}
  function openPlan(u){FCK.modal({title:`Abonnement — ${u.full_name}`,html:`<form id="planForm" class="form-grid"><div class="field full"><label>Plan à activer</label><select class="select" name="plan"><option value="free" ${u.plan==='free'?'selected':''}>Free — 10 jours</option><option value="standard" ${u.plan==='standard'?'selected':''}>Standard — 30 jours — 5 100 F</option><option value="business" ${u.plan==='business'?'selected':''}>Business — 365 jours — 45 600 F</option></select></div><div class="alert alert-warn field full">L’activation démarre à la date de validation et remplace la date d’expiration actuelle.</div><div class="field full"><button class="btn btn-primary" type="submit">Activer ce plan</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#planForm').addEventListener('submit',async e=>{e.preventDefault();const plan=new FormData(e.currentTarget).get('plan');try{const r=await FCK.api('/api/superadmin',{method:'POST',body:JSON.stringify({action:'set-plan',id:u.id,plan})});FCK.toast(r.message);close();await load()}catch(err){FCK.toast(err.message)}})})}
  function openReset(u,requestId=''){FCK.modal({title:`Réinitialiser — ${u?.full_name||u?.email||'Compte'}`,html:`<form id="superReset" class="form-grid"><div class="field full"><label>Nouveau mot de passe temporaire</label><input class="input" type="password" name="password" minlength="8" required></div><div class="alert alert-warn field full">Toutes les sessions existantes de ce compte seront invalidées.</div><div class="field full"><button class="btn btn-danger" type="submit">Réinitialiser le mot de passe</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#superReset').addEventListener('submit',async e=>{e.preventDefault();const password=new FormData(e.currentTarget).get('password');try{const r=await FCK.api('/api/superadmin',{method:'POST',body:JSON.stringify(requestId?{action:'resolve-reset',request_id:requestId,password}:{action:'reset-password',id:u.id,password})});FCK.toast(r.message||'Mot de passe réinitialisé.');close();await load()}catch(err){FCK.toast(err.message)}})})}
  function accessChecks(a={}){return `<div class="checkboxes"><label class="check"><input type="checkbox" name="sectors" ${a.sectors!==false?'checked':''}> Secteur</label><label class="check"><input type="checkbox" name="responsibles" ${a.responsibles!==false?'checked':''}> Responsables</label><label class="check"><input type="checkbox" name="girls" ${a.girls!==false?'checked':''}> Jeunes filles</label><label class="check"><input type="checkbox" name="boys" ${a.boys!==false?'checked':''}> Jeunes garçons</label></div>`}
  function openAccess(u){FCK.modal({title:`Accès — ${u.full_name}`,html:`<form id="superAccess" class="form-grid"><div class="field full">${accessChecks(u.access)}</div><div class="field full"><button class="btn btn-primary">Enregistrer</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#superAccess').addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget,access={home:true,settings:true,sectors:f.elements.sectors.checked,responsibles:f.elements.responsibles.checked,girls:f.elements.girls.checked,boys:f.elements.boys.checked};try{await FCK.api('/api/superadmin',{method:'POST',body:JSON.stringify({action:'update-access',id:u.id,access})});FCK.toast('Accès mis à jour.');close();await load()}catch(err){FCK.toast(err.message)}})})}
  function renderResets(){const list=d.reset_requests||[];document.getElementById('superResets').innerHTML=`<table><thead><tr><th>Date</th><th>Nom</th><th>E-mail</th><th>Type</th><th>Traitement</th></tr></thead><tbody>${list.map(r=>`<tr><td>${FCK.fmtDate(r.requested_at)}</td><td>${FCK.esc(r.full_name||'—')}</td><td>${FCK.esc(r.email)}</td><td><span class="badge badge-blue">${FCK.esc(r.target_role)}</span></td><td>${r.target_role==='admin'?`<button class="btn btn-sm btn-danger" data-rid="${r.id}">Réinitialiser</button>`:'<span class="hint">À traiter par l’Administrateur</span>'}</td></tr>`).join('')||'<tr><td colspan="5">Aucune demande en attente.</td></tr>'}</tbody></table>`;document.querySelectorAll('[data-rid]').forEach(b=>b.addEventListener('click',()=>{const r=list.find(x=>x.id===b.dataset.rid);openReset(r,r.id)}))}
  function renderAudit(){const list=d.audit||[];document.getElementById('auditTable').innerHTML=`<table><thead><tr><th>Date</th><th>Rôle</th><th>Action</th><th>Cible</th><th>IP</th></tr></thead><tbody>${list.map(a=>`<tr><td>${FCK.fmtDate(a.created_at)}</td><td>${FCK.esc(a.actor_role||'—')}</td><td>${FCK.esc(a.action)}</td><td>${FCK.esc(a.target_type||'—')} ${FCK.esc(a.target_id||'')}</td><td>${FCK.esc(a.ip_address||'—')}</td></tr>`).join('')||'<tr><td colspan="5">Journal vide.</td></tr>'}</tbody></table>`}
  document.addEventListener('fck:ready',e=>{if(e.detail)load()});
})();
