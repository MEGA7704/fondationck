(()=>{
  let data=null; const panels={account:'panelAccount',subscription:'panelSubscription',users:'panelUsers',content:'panelContent',news:'panelNews',resets:'panelResets',messages:'panelMessages'};
  const labels={account:'Mon compte',subscription:'Abonnement',users:'Utilisateurs & accès',content:'Présentation du site',news:'Nouvelles du jour',resets:'Mots de passe oubliés',messages:'Messages reçus'};
  const isAdmin=()=>data&&['admin','superadmin'].includes(data.user.role);
  function activate(name){document.querySelectorAll('.panel').forEach(p=>p.classList.remove('active'));document.getElementById(panels[name])?.classList.add('active');document.querySelectorAll('#settingsSubnav button').forEach(b=>b.classList.toggle('active',b.dataset.panel===name));if(name==='subscription')history.replaceState(null,'','#abonnement')}
  function renderNav(){const names=['account','subscription',...(isAdmin()?['users','content','news','resets','messages']:[])];const nav=document.getElementById('settingsSubnav');nav.innerHTML=names.map((n,i)=>`<button type="button" data-panel="${n}" class="${i===0?'active':''}">${labels[n]}</button>`).join('');nav.querySelectorAll('button').forEach(b=>b.addEventListener('click',()=>activate(b.dataset.panel)));if(location.hash==='#abonnement')activate('subscription')}
  function renderAccount(){const u=data.user,p=data.plan||{};document.getElementById('panelAccount').innerHTML=`<div class="settings-grid"><section class="settings-card"><h2>Informations du compte</h2><div class="form-grid"><div class="field"><label>Nom</label><div class="input">${FCK.esc(u.full_name)}</div></div><div class="field"><label>E-mail</label><div class="input">${FCK.esc(u.email)}</div></div><div class="field"><label>Rôle</label><div><span class="badge badge-blue">${FCK.esc(u.role)}</span></div></div><div class="field"><label>Statut</label><div><span class="badge ${u.status==='active'?'badge-green':'badge-red'}">${FCK.esc(u.status)}</span></div></div></div><div class="alert ${data.subscription_active?'alert-success':'alert-danger'}">Plan <strong>${FCK.esc(u.plan)}</strong> · expiration : <strong>${FCK.fmtDate(u.plan_expires_at)}</strong> · ${Number(p.days_remaining||0)} jour(s) restant(s).</div></section><section class="settings-card"><h2>Modifier mon mot de passe</h2><form id="changePwd" class="form-grid"><div class="field full"><label>Mot de passe actuel</label><input class="input" type="password" name="current_password" required></div><div class="field full"><label>Nouveau mot de passe</label><input class="input" type="password" name="new_password" minlength="8" required></div><div class="field full"><button class="btn btn-primary" type="submit">Modifier le mot de passe</button></div></form><p class="hint">Après modification, toutes vos sessions sont automatiquement invalidées.</p></section></div>`;
    document.getElementById('changePwd').addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));try{const r=await FCK.save('change-own-password',f);FCK.toast(r.message);setTimeout(()=>location.href='/connexion.html',600)}catch(err){FCK.toast(err.message)}})
  }
  function renderSubscription(){const current=data.user.plan;document.getElementById('panelSubscription').innerHTML=`<div class="section-head"><div><h2>Formules d’abonnement</h2><p>Les statuts d’abonnement sont protégés côté serveur et ne peuvent être modifiés que par le Super Admin.</p></div></div><div class="plan-grid"><div class="plan"><h3>Free</h3>${current==='free'?'<span class="badge badge-green plan-current">Actuel</span>':''}<div class="price">0 F <small>/ 10 jours</small></div><ul><li>Accès complet pendant 10 jours</li><li>Rappel professionnel à la connexion</li><li>Rappel toutes les 15 minutes</li></ul><button class="btn btn-ghost" disabled>Plan d’essai</button></div><div class="plan featured"><h3>Standard</h3>${current==='standard'?'<span class="badge badge-green plan-current">Actuel</span>':''}<div class="price">5 100 F <small>/ 30 jours</small></div><ul><li>Accès complet</li><li>Durée : 30 jours</li><li>Activation après validation</li></ul><a class="btn btn-orange" target="_blank" rel="noopener" href="https://pay.wave.com/m/M_ci_Enx-2JNAklk-/c/ci/?amount=5100">Payer avec Wave</a></div><div class="plan"><h3>Business</h3>${current==='business'?'<span class="badge badge-green plan-current">Actuel</span>':''}<div class="price">45 600 F <small>/ 365 jours</small></div><ul><li>Accès complet</li><li>Durée : 365 jours</li><li>Activation annuelle</li></ul><a class="btn btn-primary" target="_blank" rel="noopener" href="https://pay.wave.com/m/M_ci_Enx-2JNAklk-/c/ci/?amount=45600">Payer avec Wave</a></div></div><div class="alert alert-info">Après paiement, l’activation du plan est effectuée dans l’espace Super Admin afin d’empêcher toute modification frauduleuse du statut d’abonnement.</div>`}
  function renderUsers(){if(!isAdmin())return;const users=(data.users||[]).filter(u=>u.role!=='superadmin');const panel=document.getElementById('panelUsers');panel.innerHTML=`<div class="table-toolbar"><div><h2>Utilisateurs et accès</h2><p class="hint">L’Administrateur crée les utilisateurs et choisit les pages qu’ils peuvent voir.</p></div><button id="addUserBtn" class="btn btn-orange">＋ Ajouter un utilisateur</button></div><div class="table-wrap"><table><thead><tr><th>Nom</th><th>E-mail</th><th>Rôle</th><th>Plan</th><th>Expire le</th><th>Accès</th><th>Actions</th></tr></thead><tbody>${users.map(u=>`<tr><td>${FCK.esc(u.full_name)}</td><td>${FCK.esc(u.email)}</td><td><span class="badge badge-blue">${FCK.esc(u.role)}</span></td><td><span class="badge ${u.plan==='free'?'badge-orange':'badge-green'}">${FCK.esc(u.plan)}</span></td><td>${FCK.fmtDate(u.plan_expires_at)}</td><td>${accessText(u.access)}</td><td><div class="actions">${u.role==='member'?`<button class="btn btn-sm btn-ghost" data-access="${u.id}">Accès</button><button class="btn btn-sm btn-outline" data-reset="${u.id}">Réinit. MDP</button>`:'<span class="hint">Géré par Super Admin</span>'}</div></td></tr>`).join('')||'<tr><td colspan="7">Aucun utilisateur.</td></tr>'}</tbody></table></div>`;
    panel.querySelector('#addUserBtn').addEventListener('click',openCreateUser);panel.querySelectorAll('[data-access]').forEach(b=>b.addEventListener('click',()=>openAccess(users.find(u=>u.id===b.dataset.access))));panel.querySelectorAll('[data-reset]').forEach(b=>b.addEventListener('click',()=>openReset(users.find(u=>u.id===b.dataset.reset))));
  }
  function accessText(a={}){const arr=[];if(a.sectors)arr.push('Secteur');if(a.responsibles)arr.push('Responsables');if(a.girls)arr.push('Filles');if(a.boys)arr.push('Garçons');return arr.length?arr.join(', '):'Aucun'}
  function accessChecks(a={}){return `<div class="checkboxes"><label class="check"><input type="checkbox" name="sectors" ${a.sectors!==false?'checked':''}> Secteur</label><label class="check"><input type="checkbox" name="responsibles" ${a.responsibles!==false?'checked':''}> Responsables</label><label class="check"><input type="checkbox" name="girls" ${a.girls!==false?'checked':''}> Jeunes filles</label><label class="check"><input type="checkbox" name="boys" ${a.boys!==false?'checked':''}> Jeunes garçons</label></div>`}
  function parseAccessForm(form){return {sectors:form.elements.sectors.checked,responsibles:form.elements.responsibles.checked,girls:form.elements.girls.checked,boys:form.elements.boys.checked,settings:true,home:true}}
  function openCreateUser(){FCK.modal({title:'Ajouter un utilisateur',html:`<form id="newUser" class="form-grid"><div class="field"><label>Nom complet *</label><input class="input" name="full_name" required></div><div class="field"><label>Téléphone</label><input class="input" name="phone"></div><div class="field full"><label>E-mail *</label><input class="input" type="email" name="email" required></div><div class="field full"><label>Mot de passe temporaire *</label><input class="input" type="password" name="password" minlength="8" required></div><div class="field full"><label>Pages visibles</label>${accessChecks({})}</div><div class="field full"><button class="btn btn-primary" type="submit">Créer l’utilisateur</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#newUser').addEventListener('submit',async e=>{e.preventDefault();const f=e.currentTarget,obj=Object.fromEntries(new FormData(f));obj.access=parseAccessForm(f);try{const r=await FCK.save('create-user',obj);FCK.toast(r.message||'Utilisateur créé.');close();await refresh()}catch(err){FCK.toast(err.message)}})})}
  function openAccess(u){FCK.modal({title:`Accès — ${u.full_name}`,html:`<form id="accessForm" class="form-grid"><div class="field full"><label>Choisissez les pages que cet utilisateur peut voir</label>${accessChecks(u.access)}</div><div class="field full"><button class="btn btn-primary" type="submit">Enregistrer les accès</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#accessForm').addEventListener('submit',async e=>{e.preventDefault();try{await FCK.save('update-user-access',{id:u.id,access:parseAccessForm(e.currentTarget)});FCK.toast('Accès mis à jour.');close();await refresh()}catch(err){FCK.toast(err.message)}})})}
  function openReset(u,requestId=''){FCK.modal({title:`Réinitialiser le mot de passe — ${u?.full_name||'Utilisateur'}`,html:`<form id="resetForm" class="form-grid"><div class="field full"><label>Nouveau mot de passe temporaire</label><input class="input" type="password" name="password" minlength="8" required></div><div class="alert alert-warn field full">Toutes les anciennes sessions seront invalidées.</div><div class="field full"><button class="btn btn-danger" type="submit">Réinitialiser</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#resetForm').addEventListener('submit',async e=>{e.preventDefault();const pwd=new FormData(e.currentTarget).get('password');try{const r=await FCK.save(requestId?'resolve-member-reset':'reset-member-password',requestId?{request_id:requestId,password:pwd}:{id:u.id,password:pwd});FCK.toast(r.message||'Mot de passe réinitialisé.');close();await refresh()}catch(err){FCK.toast(err.message)}})})}
  function renderContent(){if(!isAdmin())return;const c=data.content||{};document.getElementById('panelContent').innerHTML=`<h2>Présentation publique de la Fondation</h2><form id="contentForm" class="form-grid"><div class="field full"><label>Présentation</label><textarea class="textarea" name="presentation">${FCK.esc(c.presentation||'')}</textarea></div><div class="field full"><label>Mission</label><textarea class="textarea" name="mission">${FCK.esc(c.mission||'')}</textarea></div><div class="field full"><label>Vision</label><textarea class="textarea" name="vision">${FCK.esc(c.vision||'')}</textarea></div><div class="field full"><label>Perspectives</label><textarea class="textarea" name="perspectives">${FCK.esc(c.perspectives||'')}</textarea></div><div class="field"><label>Téléphone</label><input class="input" name="contact_phone" value="${FCK.esc(c.contact_phone||'')}"></div><div class="field"><label>WhatsApp</label><input class="input" name="whatsapp" value="${FCK.esc(c.whatsapp||'')}"></div><div class="field"><label>E-mail</label><input class="input" type="email" name="contact_email" value="${FCK.esc(c.contact_email||'')}"></div><div class="field"><label>Adresse</label><input class="input" name="address" value="${FCK.esc(c.address||'')}"></div><div class="field full"><button class="btn btn-primary" type="submit">Enregistrer la présentation</button></div></form>`;document.getElementById('contentForm').addEventListener('submit',async e=>{e.preventDefault();try{await FCK.save('update-site-content',Object.fromEntries(new FormData(e.currentTarget)));FCK.toast('Présentation mise à jour.');await refresh()}catch(err){FCK.toast(err.message)}})}
  function renderNews(){if(!isAdmin())return;const news=data.news||[];const p=document.getElementById('panelNews');p.innerHTML=`<div class="table-toolbar"><div><h2>Nouvelles du jour</h2><p class="hint">Chaque publication crée une carte sur l’accueil et une page d’information.</p></div><button id="addNews" class="btn btn-orange">＋ Nouvelle publication</button></div><div class="table-wrap"><table><thead><tr><th>Image</th><th>Titre</th><th>Publication</th><th>Statut</th><th>Actions</th></tr></thead><tbody>${news.map(n=>`<tr><td>${n.image_url?`<img src="${n.image_url}" alt="" style="width:72px;height:48px;object-fit:cover;border-radius:8px">`:'—'}</td><td>${FCK.esc(n.title)}</td><td>${FCK.fmtDate(n.published_at)}</td><td><span class="badge ${n.published?'badge-green':'badge-gray'}">${n.published?'Publié':'Masqué'}</span></td><td><div class="actions"><button class="btn btn-sm btn-ghost" data-edit-news="${n.id}">Modifier</button><button class="btn btn-sm btn-danger" data-del-news="${n.id}">Supprimer</button></div></td></tr>`).join('')||'<tr><td colspan="5">Aucune publication.</td></tr>'}</tbody></table></div>`;p.querySelector('#addNews').addEventListener('click',()=>openNews());p.querySelectorAll('[data-edit-news]').forEach(b=>b.addEventListener('click',()=>openNews(news.find(n=>n.id===b.dataset.editNews))));p.querySelectorAll('[data-del-news]').forEach(b=>b.addEventListener('click',()=>deleteNews(b.dataset.delNews)))}
  function openNews(n={}){FCK.modal({title:n.id?'Modifier la publication':'Créer une publication',large:true,html:`<form id="newsForm" class="form-grid"><div class="field full"><label>Titre *</label><input class="input" name="title" value="${FCK.esc(n.title||'')}" required></div><div class="field full"><label>Résumé</label><textarea class="textarea" name="summary">${FCK.esc(n.summary||'')}</textarea></div><div class="field full"><label>Contenu *</label><textarea class="textarea" name="content" style="min-height:220px" required>${FCK.esc(n.content||'')}</textarea></div><div class="field"><label>Image (JPG, PNG, WEBP · 5 Mo max)</label><input class="input" type="file" name="image" accept="image/jpeg,image/png,image/webp"></div><div class="field"><label>Publication</label><label class="check"><input type="checkbox" name="published" ${n.published!==0?'checked':''}> Afficher sur l’accueil</label></div>${n.image_url?`<div class="field full"><img src="${n.image_url}" alt="" style="max-width:260px;border-radius:12px"></div>`:''}<div class="field full"><button class="btn btn-primary" type="submit">Enregistrer</button></div></form>`,onReady:(wrap,close)=>wrap.querySelector('#newsForm').addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,fd=new FormData(form);let imageKey=n.image_key||'';const file=fd.get('image');try{if(file&&file.size){const up=new FormData();up.append('image',file);const ur=await FCK.api('/api/upload-image',{method:'POST',body:up});imageKey=ur.key}const obj={title:fd.get('title'),summary:fd.get('summary'),content:fd.get('content'),image_key:imageKey,published:form.elements.published.checked};if(n.id)obj.id=n.id;await FCK.save(n.id?'update-news':'add-news',obj);FCK.toast('Publication enregistrée.');close();await refresh()}catch(err){FCK.toast(err.message)}})})}
  async function deleteNews(id){if(!confirm('Supprimer cette publication ?'))return;try{await FCK.save('delete-news',{id});FCK.toast('Publication supprimée.');await refresh()}catch(err){FCK.toast(err.message)}}
  function renderResets(){if(!isAdmin())return;const list=data.reset_requests||[];const p=document.getElementById('panelResets');p.innerHTML=`<h2>Demandes de réinitialisation des utilisateurs</h2><div class="alert alert-info">Les demandes des utilisateurs sont traitées par votre Administrateur. Les demandes des Administrateurs apparaissent dans l’espace Super Admin.</div><div class="table-wrap"><table><thead><tr><th>Nom</th><th>E-mail</th><th>Demandé le</th><th>Action</th></tr></thead><tbody>${list.map(r=>`<tr><td>${FCK.esc(r.full_name||'Utilisateur')}</td><td>${FCK.esc(r.email)}</td><td>${FCK.fmtDate(r.requested_at)}</td><td><button class="btn btn-sm btn-danger" data-resolve="${r.id}">Réinitialiser</button></td></tr>`).join('')||'<tr><td colspan="4">Aucune demande en attente.</td></tr>'}</tbody></table></div>`;p.querySelectorAll('[data-resolve]').forEach(b=>b.addEventListener('click',()=>{const req=list.find(r=>r.id===b.dataset.resolve);openReset({full_name:req.full_name},req.id)}))}
  function renderMessages(){if(!isAdmin())return;const list=data.contact_messages||[];document.getElementById('panelMessages').innerHTML=`<h2>Messages reçus</h2><div class="table-wrap"><table><thead><tr><th>Date</th><th>Nom</th><th>Contact</th><th>Objet</th><th>Message</th></tr></thead><tbody>${list.map(m=>`<tr><td>${FCK.fmtDate(m.created_at)}</td><td>${FCK.esc(m.name)}</td><td>${FCK.esc(m.phone||m.email||'—')}</td><td>${FCK.esc(m.subject||'—')}</td><td style="white-space:normal;min-width:280px">${FCK.esc(m.message)}</td></tr>`).join('')||'<tr><td colspan="5">Aucun message.</td></tr>'}</tbody></table></div>`}
  function renderAll(){renderNav();renderAccount();renderSubscription();if(isAdmin()){renderUsers();renderContent();renderNews();renderResets();renderMessages()}if(location.hash==='#abonnement')activate('subscription')}
  async function refresh(){data=await FCK.loadData(true);renderAll()}
  document.addEventListener('fck:ready',e=>{data=e.detail;if(data)renderAll()});
})();
